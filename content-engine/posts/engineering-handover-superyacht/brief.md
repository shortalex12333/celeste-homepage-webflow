# Brief — engineering-handover-superyacht

> Generated by brief.mjs on 2026-06-11. This is the spec **Claude drafts against**.
> Claude writes `posts/engineering-handover-superyacht/draft.html` (BODY html + `{{asset:id}}` placeholders), then it passes `gate.mjs`, then the founder gates `publish.mjs`.

## Target

- **Keyword:** `chief engineer handover checklist`
- **Chapter:** `handover`
- **Format:** guide
- **Slug / URL:** `/blogs/engineering-handover-superyacht`
- **Retarget of:** `/blogs/engineering-handover-superyacht` (do NOT change the slug — it is the trusted asset)
- **Search intent:** How-to / informational: the reader wants to *do* the thing in "chief engineer handover checklist"; lead with the practical steps, land the product as the payoff.

## SERP angle (from REFRESH-PLAN.md)

- **Title direction:** Chief Engineer Handover Checklist for Superyachts: 7 Domains a Proper Handover Covers — Celeste
- **Plan verdict:** retarget-refresh
- **GSC signal:** 97 impr · pos 7.1 · 3 clicks — best URL on site

  Per-post spec excerpt (read the full block in REFRESH-PLAN.md before drafting):

  - Title: "Chief Engineer Handover Checklist for Superyachts: 7 Domains a Proper Handover Covers — Celeste"; H1 "The Chief Engineer Handover Checklist Most Yachts Don't Have"; meta ≤155 chars containing "chief engineer handover checklist" + "engine room".
  - Convert the 7-domain section to `<ol>` + H3 per domain under exact-match H2; add ItemList JSON-LD.
  - NEW H2 "Engine room handover checklist: applying the seven domains" (150–250 words: generators, watermakers, bilge/fire config, alarm thresholds) — captures the secondary keyword on the same URL (ALE-232 new-posts #2 and #7 are served here; see §D).
  - Truth fixes per §B (lines 224, 210, 143, 193).
  - FAQ (4 Qs) + FAQPage schema. Product links: `/handover` at the two-signature image, `/search` at the search paragraph.
  - Renders: `handover-draft-drawer.png` in the checklist section; `search-results-grouped.png` at the search paragraph; `ledger-activity-drawer.png` at the immutable-record paragraph. Keep `art2-handover-signatures.png` + `art2-fault-entities.png`. Commission a cert render slot for domain 5 only if `certificate-lens.png` doesn't fit.
  - Optional high-leverage: printable one-page 7-domain checklist (HTML print view, no gate).
  - Absorb the surviving substance of `auto-generated-handover` (the 24-items/5-departments example and the live-links wormhole passage, rewritten to "captured as you work / every entry resolves to a live record") under a short H2 "What the compiled draft contains".

## SAFE claim phrasings available for this chapter

Use these **verbatim or trivially inflected**. They are the only product claims pre-cleared for this chapter. Anything stronger must be added to the allowlist first.

- Captured as you work.
- Every record an engineer closes — a fault, a work order, a note — offers a single tap to add it to the handover draft.
- The draft accumulates as the engineer works.
- Months of context accumulate as a by-product of normal work.
- Never asked twice if dismissed.
- The outgoing engineer reviews the draft, then signs. The incoming engineer acknowledges: 'I have read and understood the operational state of this vessel.'
- Both signatures timestamped. Locked after dual signature.
- Every entry resolves back to a live record — one click opens the fault, the work order, the part.
- The handover is a live index into the vessel's operational data, not a static document.
- The incoming engineer inherits context, not chaos.
- Built from one-tap captures as the engineer works — reviewed, then signed by both parties.

## Do NOT write (chapter banned table — the gate hard-fails these)

| Banned phrasing | Why | Write instead |
|---|---|---|
| "The handover writes itself" / "writes itself" (any subject) | Reality = 1-tap capture from a queue into a draft. Nothing writes itself. | Captured as you work. |
| "auto-generated" / "auto-generates" / "generated automatically" | Same. | Built from one-tap captures, reviewed before signing. |
| "assembled automatically" / "compiled automatically" / "assembles itself" / "not assembled manually" | Same. | The draft accumulates as the engineer works. |
| "The departing engineer does not need to remember what to include. The system already has it." | Implies full auto-assembly; capture requires the engineer's tap. | Each tap while working is one less thing to remember at rotation. |
| "Rough notes become a structured handover" | Implies transformation without the capture step. | One-tap captures become the handover draft. |
| "You review, we create" | "We create" = auto-write framing. | You capture as you work. You review. Both sides sign. |
| "Generated from operational data — not memory" | "Generated" framing. | Compiled from the entries the engineer captured — not from memory. |

Plus the GLOBAL gate: no AI/smart/automate/seamless/optimize/magic/revolutionary lexicon; no "writes itself / every domain / sealed PDF / sub-second / real-time" overclaims; **no pricing or numbers** (CALL-ONLY); the host is `verifier.celeste7.ai` (never `verify.`).

## Suggested assets for this chapter (4)

Drop the placeholder `{{asset:<id>}}` where you want each image; `publish.mjs` resolves it to a `<figure>` with the approved alt and copies the file to the served path. **One media = one focal point per section.**

### {{asset:handover-draft}}

- **Shows:** The handover draft drawer in CelesteOS: seven items grouped by day — a critical coolant fault, an oil-change work order, a watermaker note — each tagged and time-stamped, with an Export Handover button at the top.
- **Supports:** "Handover captured as you work — items collect into a draft day by day"; "export when ready"; "nothing retyped at crew change"
- **NEVER:**
  - NEVER "writes itself"
  - NEVER "auto-generated"
  - NEVER "auto-written"
- **Usage note:** NEVER "writes itself" / "auto-generated" / "auto-written". Reality: 1-tap capture from a queue into the draft. Tall right-hand drawer crop (460×900); some dark space below the list is the real drawer surface.

### {{asset:handover-export}}

- **Shows:** Cover page of a CelesteOS technical handover report: document number, generation date, the period covered, and a contents list by department.
- **Supports:** "The draft exports to a formal, dated handover report"; "covers N items across departments"; "reads like a document a surveyor can hold"
- **NEVER:**
  - never caption the CONTENT as auto-written
  - Do not call the export "sealed PDF" (not live).
- **Usage note:** "Generated" on the cover = export timestamp; never caption the CONTENT as auto-written. Do not call the export "sealed PDF" (not live). First-page viewport crop of a longer document.

### {{asset:handover-signatures}}

- **Shows:** The handover sign-off in CelesteOS: outgoing and incoming chief engineers' signatures side by side, the incoming chief's written acknowledgement above, and a note that the captain was notified at sign-off.
- **Supports:** "Outgoing/incoming two-signature flow (live)"; "incoming chief acknowledges in writing"; "captain notified at sign-off — no one can claim they weren't told"; "receipt independently verifiable on the verifier"
- **NEVER:**
  - do NOT extend to "sealed PDF"
- **Usage note:** Hostname in-image was corrected to verifier.celeste7.ai before capture (source HTML says verify. — dead host; fix upstream before re-rendering). "Sealed" here refers to the receipt, which is live — do NOT extend to "sealed PDF".

### {{asset:handover-linked}}

- **Shows:** A single handover entry in CelesteOS with its linked records panel: the original fault, the work order with labour hours, the replacement part, and the OEM bulletin cited in the diagnosis — each one a link into the live record.
- **Supports:** "The handover wormhole (live): every entry links to the live record"; "incoming chief reads, clicks, lands in the fault/WO/part/document itself"
- **NEVER:** (no special restriction recorded — global gate still applies)
- **Usage note:** The in-image caption "Every entry in the handover behaves like this" is approved. Card crop 720×520.

## Recommended internal links

- "crew turnover and knowledge loss" → `/blogs/knowledge-crisis-superyachts`  — flagship handover pillar
- the handover → `/handover`  — product page (two-signature image)
- request pilot access → `/#pilot`  — conversion CTA

## Required structure

- **H1:** one exact-or-near-match of the keyword; honest, specific, no hype.
- **Lede / deck:** Apple-style — first sentence carries the claim (can be white-emphasis on render), rest is context.
- **Sections (H2 + ordered H3 steps):** the practical how-to is the bulk; use `<ol>` for steps (drives ItemList schema).
- **Payoff section:** the product as the reason the manual work stops mattering — one suggested asset.
- **FAQ (H2 + 3–4 question H3s):** match People-Also-Ask; FAQPage JSON-LD in meta.json.
- **Summary list:** `<ul class="summary-list">` of the post's honest takeaways.
- **Sources:** cite external claims with a numbered `<div class="article-sources">` (E-E-A-T).

### Closing CTA

- End on a calm, short CTA. Conversion link → `/#pilot` (request pilot access). For proof/compliance angles, link → `/trust`.
- No pricing. No urgency theatre. The page converts traffic; the founder converts the prospect.

## Drafting checklist (before you hand off to the gate)

- [ ] Exactly one `<h1>` (the rest are `<h2>/<h3>`).
- [ ] Body uses only the SAFE claims above (or weaker).
- [ ] Every image is a `{{asset:id}}` placeholder from the suggested list (or another registry id), never a raw `<img>`.
- [ ] No banned lexicon / overclaim / pricing; host is `verifier.celeste7.ai`.
- [ ] FAQ section present with question `<h3>`s (drives FAQPage schema in meta.json).
- [ ] `meta.json`: title 30–65 chars, description ≤155 chars, JSON-LD present, `updated` set.
- [ ] Run `node gate.mjs posts/engineering-handover-superyacht` until clean.

