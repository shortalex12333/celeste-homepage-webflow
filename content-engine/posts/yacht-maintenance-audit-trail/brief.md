# Brief — yacht-maintenance-audit-trail

> Generated by brief.mjs on 2026-06-11. This is the spec **Claude drafts against**.
> Claude writes `posts/yacht-maintenance-audit-trail/draft.html` (BODY html + `{{asset:id}}` placeholders), then it passes `gate.mjs`, then the founder gates `publish.mjs`.

## Target

- **Keyword:** `yacht maintenance audit trail`
- **Chapter:** `ledger`
- **Format:** pillar
- **Slug / URL:** `/blogs/yacht-maintenance-audit-trail`
- **Search intent:** Top-of-funnel pillar: "yacht maintenance audit trail" is a broad problem term; own the topic, link the cluster, convert the patient reader.

## SAFE claim phrasings available for this chapter

Use these **verbatim or trivially inflected**. They are the only product claims pre-cleared for this chapter. Anything stronger must be added to the allowlist first.

- The ledger is append-only. Nothing is ever overwritten.
- Corrections are recorded as new entries referencing the original. The original remains intact.
- Every action — logging a fault, creating a work order, signing a handover — is attributed and timestamped.
- Who acted, what they did, when, and which record was affected.
- Open any record and read its full audit trail.
- Deleted rows are struck through, never purged — the trail is the trail.
- The audit trail is not a feature. It is a legal defence.
- Records that can be retroactively modified cannot serve as evidence.
- Audit prep becomes retrieval, not assembly.

## Do NOT write (chapter banned table — the gate hard-fails these)

| Banned phrasing | Why | Write instead |
|---|---|---|
| "Hop back into anyone's work" / "pick a record back up days later" / any Chrome-history / resume framing | No resume capability is shipped. | Open any record and read its full audit trail — who touched what, when. |
| "Sealed PDF" / "audit trail exportable as a structured PDF" | Planned (v1.0), not live. | Every record's history is available on demand, and independently verifiable at verifier.celeste7.ai. |
| "The system records who opened it, when, and for how long" / "measured duration" / "whether they read it in full" | Read-duration / read-completion tracking is NOT in the verified-shipped set. | Actions on records are logged — created, updated, signed, linked. (NEEDS-EVIDENCE before any read-event claim) |
| "Read events logged — evidence of awareness" | Same — NEEDS-EVIDENCE. | Omit, or scope to actions. |
| "records interactions automatically" | Auto-stem; also implies passive read-tracking. | Actions are recorded as the crew works — no checkbox, no extra step. |
| "One click on Generate warranty claim pack" / "audit-grade pack in seconds" | Money-finder/claim-pack territory — pull-on-open, not buyer-visible; do not depict. | Warranty history and its evidence live on the record. (roadmap tease only, clearly labelled) |
| "named individual" as a universal claim | Live attribution defaults to role (Captain/Chief); personal-name display is gated. | attributed — by signed-in user and role — and timestamped |

Plus the GLOBAL gate: no AI/smart/automate/seamless/optimize/magic/revolutionary lexicon; no "writes itself / every domain / sealed PDF / sub-second / real-time" overclaims; **no pricing or numbers** (CALL-ONLY); the host is `verifier.celeste7.ai` (never `verify.`).

## Suggested assets for this chapter (1)

Drop the placeholder `{{asset:<id>}}` where you want each image; `publish.mjs` resolves it to a `<figure>` with the approved alt and copies the file to the served path. **One media = one focal point per section.**

### {{asset:ledger}}

- **Shows:** The CelesteOS ledger drawer: a day-by-day activity timeline — a work order status change, a note added to a fault, a purchase order created — each entry attributed and time-stamped.
- **Supports:** "Append-only activity record"; "every change attributed to a person and a time"; "reads/edits counted per day"
- **NEVER:**
  - NEVER "hop back / Chrome-history resume" (no resume).
  - NEVER "sealed PDF".
- **Usage note:** NEVER "hop back / Chrome-history resume" (no resume). NEVER "sealed PDF". Append-only is the safe claim. Drawer crop ~467×900.

## Recommended internal links

- "append-only audit trail" → `/blogs/audit-trail-superyacht`  — chapter pillar/owner — link in, do not compete on its keyword
- "crew turnover and knowledge loss" → `/blogs/knowledge-crisis-superyachts`  — flagship handover pillar
- verify a sample receipt → `https://verifier.celeste7.ai`  — the proof payoff (host is verifier., never verify.)
- verifiable, auditable maintenance records → `/blogs/warranty-claim-documentation`  — Tier-A money page
- request pilot access → `/#pilot`  — conversion CTA

## Required structure

- **H1:** one exact-or-near-match of the keyword; honest, specific, no hype.
- **Lede / deck:** Apple-style — first sentence carries the claim (can be white-emphasis on render), rest is context.
- **Sections (H2):** 4–6 problem→consequence→what-good-looks-like sections; each cluster post links in once.
- **Evidence section:** one product moment with a suggested asset — show, do not claim.
- **FAQ (H2 + 3–4 question H3s):** match People-Also-Ask; FAQPage JSON-LD in meta.json.
- **Summary list:** `<ul class="summary-list">` of the post's honest takeaways.
- **Sources:** cite external claims with a numbered `<div class="article-sources">` (E-E-A-T).

### Closing CTA

- End on a calm, short CTA. Conversion link → `/#pilot` (request pilot access). For proof/compliance angles, link → `/trust`.
- No pricing. No urgency theatre. The page converts traffic; the founder converts the prospect.

## Drafting checklist (before you hand off to the gate)

- [ ] Exactly one `<h1>` (the rest are `<h2>/<h3>`).
- [ ] Body uses only the SAFE claims above (or weaker).
- [ ] Every image is a `{{asset:id}}` placeholder from the suggested list (or another registry id), never a raw `<img>`.
- [ ] No banned lexicon / overclaim / pricing; host is `verifier.celeste7.ai`.
- [ ] FAQ section present with question `<h3>`s (drives FAQPage schema in meta.json).
- [ ] `meta.json`: title 30–65 chars, description ≤155 chars, JSON-LD present, `updated` set.
- [ ] Run `node gate.mjs posts/yacht-maintenance-audit-trail` until clean.

